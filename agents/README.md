# Agent integrations

**Current focus:** OpenClaw — native plugin package with CLI-based skills under [openclaw/](openclaw/).

| Agent | Folder | Overview |
|---|---|---|
| OpenClaw | [openclaw/](openclaw/) | **Primary:** `zerogpu` CLI + package/plugin id **`zerogpu-openclaw-plugin`** + CLI-based skills |
| Claude Code | [claude/](claude/) | `zerogpu` CLI + marketplace plugin + `SKILL.md` skills |

Both integrations run the `zerogpu` CLI locally through the agent's Bash tools — nothing to host or register. Keys and dashboards: [platform.zerogpu.ai](https://platform.zerogpu.ai).
