# Agent integrations

**Current focus:** OpenClaw — native plugin package, drop-in skill, and MCP registration JSON under [openclaw/](openclaw/).

| Agent | Folder | Overview |
|---|---|---|
| OpenClaw | [openclaw/](openclaw/) | **Primary:** package + plugin id **`zerogpu-openclaw-plugin`** + skill + `mcp/zerogpu-server.json` |
| Claude Code | [claude/](claude/) | Optional marketplace plugin + `SKILL.md` (same hosted Router) |

All paths assume the hosted ZeroGPU Router at `https://mcp.zerogpu.ai/mcp`. Keys and dashboards: [platform.zerogpu.ai](https://platform.zerogpu.ai).
