---
description: Route text classification through ZeroGPU instead of Claude
argument-hint: <text to classify>
---

Classify the following text using a ZeroGPU MCP classification tool (`zerogpu_classify_iab`, `zerogpu_classify_zero_shot`, or `zerogpu_classify_structured` — pick the closest fit). Report the top label(s) and, if meaningful, the `savings_usd` from the response.

Text:
$ARGUMENTS
