---
description: Route summarization through ZeroGPU instead of Claude
argument-hint: <text to summarize>
---

Call the `zerogpu_summarize` MCP tool on the text below and return the `summary` field. Mention `savings_usd` from the response if meaningful.

Text:
$ARGUMENTS
